cmake_minimum_required(VERSION 3.14.1)
set(TARGET_EXECUTABLE "DataTypeTest")

set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${PROJECT_SOURCE_DIR}/dist/${TARGET_EXECUTABLE})

file(GLOB_RECURSE SOURCE_FILES ./*.cpp)

add_executable(${TARGET_EXECUTABLE} ${SOURCE_FILES})
target_link_libraries(${TARGET_EXECUTABLE} ${MXSTREAM_TEST_COMMON_DEP_LIBS})

install(FILES CMakeLists.txt DESTINATION ${PROJECT_SOURCE_DIR}/dist/${TARGET_EXECUTABLE})

add_test(NAME ${TARGET_EXECUTABLE}
        COMMAND ${TARGET_EXECUTABLE} --gtest_output=xml
        WORKING_DIRECTORY ${CMAKE_RUNTIME_OUTPUT_DIRECTORY})
