set(TARGET_EXECUTABLE "SetDeviceIdTest")

set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${PROJECT_SOURCE_DIR}/dist/SetDeviceId)

file(GLOB_RECURSE SOURCE_FILES src/*.cpp)

add_executable(${TARGET_EXECUTABLE} ${SOURCE_FILES})
target_link_libraries(${TARGET_EXECUTABLE} ${MXPLUGINS_HLT_TEST_COMMON_DEP_LIBS})

set(PIPELINES ${PROJECT_SOURCE_DIR}/SetDeviceId/pipelines)
file(GLOB_RECURSE MATERIALS *.jpg)

install(DIRECTORY ${PIPELINES} DESTINATION ${CMAKE_RUNTIME_OUTPUT_DIRECTORY})
install(FILES ${MATERIALS} DESTINATION ${CMAKE_RUNTIME_OUTPUT_DIRECTORY})

add_test(NAME ${TARGET_EXECUTABLE}
        COMMAND ${TARGET_EXECUTABLE} --gtest_output=xml
        WORKING_DIRECTORY ${CMAKE_RUNTIME_OUTPUT_DIRECTORY})
