set(TARGET_EXECUTABLE "KeepAspectRatioResizerModelTest")

set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${PROJECT_SOURCE_DIR}/dist/KeepAspectRatioResizerModel)

file(GLOB_RECURSE SRCS KeepAspectRatioResizerModelTest.cpp)
add_executable(${TARGET_EXECUTABLE} ${SRCS})
target_link_libraries(${TARGET_EXECUTABLE} ${MXPLUGINS_HLT_TEST_COMMON_DEP_LIBS})

file(GLOB_RECURSE TEST_FILES *.jpg)
file(GLOB_RECURSE TEST_FILE config/logging.conf)
file(GLOB_RECURSE PIPELINE_FILES pipeline/*.pipeline)
file(GLOB_RECURSE OUTPUT_FILES expect_json/*.json)

install(FILES ${TEST_FILES} DESTINATION ${CMAKE_RUNTIME_OUTPUT_DIRECTORY})
install(FILES ${TEST_FILE} DESTINATION ${CMAKE_RUNTIME_OUTPUT_DIRECTORY})
install(FILES ${PIPELINE_FILES} DESTINATION ${CMAKE_RUNTIME_OUTPUT_DIRECTORY}/pipeline)
install(FILES ${OUTPUT_FILES} DESTINATION ${CMAKE_RUNTIME_OUTPUT_DIRECTORY}/expect_json)

add_test(NAME ${TARGET_EXECUTABLE}
        COMMAND ${TARGET_EXECUTABLE} --gtest_output=xml
        WORKING_DIRECTORY ${CMAKE_RUNTIME_OUTPUT_DIRECTORY})
